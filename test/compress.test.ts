import{describe,it,expect}from"vitest";import{compressSchema,compressManifest}from"../src/compress.js";
describe("compressSchema",()=>{it("removes description",()=>{const{compressed}=compressSchema({type:"object",description:"unnecessary",properties:{x:{type:"string","default":"x",description:"long desc"}}});const ps=compressed.properties as any;expect(ps.x.description).toBeUndefined();});it("removes default",()=>{const{compressed}=compressSchema({type:"object",properties:{x:{type:"string","default":"hello"}}});expect((compressed.properties as any).x.default).toBeUndefined();});it("reports savings",()=>{const{stats}=compressSchema({type:"object",properties:{a:{type:"string",description:"This is a very long description that adds many tokens"},b:{type:"number","default":42,examples:[1,2,3],description:"Another description"}}});expect(stats.savingsPercent).toBeGreaterThan(0);});it("preserves required",()=>{const{compressed}=compressSchema({type:"object",properties:{x:{type:"string"}},required:["x"]});expect(compressed.required).toEqual(["x"]);});});
describe("compressManifest",()=>{it("compresses all tools",()=>{const tools=[{name:"a",description:"Tool A",inputSchema:{type:"object",properties:{x:{type:"string",description:"the x coordinate"}}}}];const r=compressManifest(tools);expect(r.totalSavings).toBeGreaterThan(0);});it("no schema tools untouched",()=>{const tools=[{name:"b"}];const r=compressManifest(tools);expect(r.totalSavings).toBe(0);});it("multiple tools",()=>{const tools=[{name:"a",inputSchema:{type:"object",properties:{x:{type:"string",description:"x"}}}},{name:"b",inputSchema:{type:"object",properties:{y:{type:"string",description:"y"}}}}];const r=compressManifest(tools);expect(r.tools).toHaveLength(2);});it("avg savings",()=>{const tools=[{name:"a",inputSchema:{type:"object",description:"big schema",properties:{x:{type:"string",description:"x",examples:[1,2],"default":"z"}}}}];const r=compressManifest(tools);expect(r.totalSavings).toBeGreaterThan(0);});});