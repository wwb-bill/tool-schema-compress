# tool-schema-compress

Compress MCP tool schemas for 50-70% fewer context tokens. 5 deterministic transforms.

```typescript
import { compressSchema } from "tool-schema-compress";
const compact = compressSchema(schema);
```

MIT
